# Persistence design docs

Populated by `/database-designer`. One file per epic: `docs/db/<epic-slug>.md` — a database schema, or, for epics whose data layer is a wiki rather than a database, the wiki page structure (page/slug naming, `[[wiki-link]]` convention, how pages are created vs. merged).
