# UI design docs

Populated by `/frontend-developer` (design phase, before implementation). One file per epic: `docs/ui/<epic-slug>.md`. If an epic has no dedicated UI, the file states that explicitly.
