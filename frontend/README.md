# Frontend

Populated by `/frontend-developer`, per `docs/ui/<epic-slug>.md`. One subfolder per epic: `frontend/<epic-slug>/`. Epics with no dedicated UI (see `docs/ui/<epic-slug>.md`) have no subfolder here.
